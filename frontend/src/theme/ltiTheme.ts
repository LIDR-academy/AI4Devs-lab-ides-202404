import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#ADD8E6",
    },
    secondary: {
      main: "#5F9EA0",
    },
    background: {
      default: "#FFFFFF",
    },
    text: {
      primary: "#333333",
    },
  },
});
